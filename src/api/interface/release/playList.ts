export namespace BasePlay {
  export interface Terminal {
    billId?: bigint;

    itemDuration?: bigint;

    itemSort?: bigint;

    listId?: bigint;

    libId?: bigint;

    listType?: bigint;

    terminalId?: bigint;
  }
}
