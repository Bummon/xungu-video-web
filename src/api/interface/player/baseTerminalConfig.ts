export namespace BaseTerminalConfig {
  export interface Config {
    configId: bigint;
    configValue: string;
    configStatus: number;
    configDescr: string;
    configSort: number;
    templateKey: string;
  }
}
