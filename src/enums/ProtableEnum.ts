/**
 * @description：ProTable使用到的一些枚举
 */
export namespace ProTableEnums {
  export enum SearchEnum {
    Input = "input",
    InputNumber = "input-number",
    Select = "select",
    SelectV2 = "select-v2",
    TreeSelect = "tree-select",
    Cascader = "cascader",
    DatePicker = "date-picker",
    TimePicker = "time-picker",
    TimeSelect = "time-select",
    Switch = "switch",
    Slider = "slider"
  }
}
