export namespace VerifyEnum {
  export enum Employe {
    empName = "员工姓名",
    empCode = "工号",
    phone = "手机",
    email = "邮箱"
  }

  export enum Profile {
    title = "名称",
    profileKey = "字段Key"
  }

  export enum Menu {
    menuName = "菜单名称",
    menuParam = "菜单权限",
    menuPath = "菜单路径"
  }

  export enum Role {
    roleName = "角色名称"
  }

  export enum User {
    nickName = "用户姓名",
    userAccount = "用户账号"
  }

  export enum App {
    appName = "应用名称",
    appKey = "编程Key"
  }

  export enum AppParam {
    paramKey = "参数Key"
  }

  export enum Dict {
    dictName = "字典名称",
    dictCode = "字典编码"
  }
}
